{
  "name": "intuitive-tarot-production",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server/server.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check server/server.mjs && node --test tests/*.test.mjs",
    "keepalive": "node scripts/keepalive.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
